---
import Layout from "../../layouts/Layout.astro";
---

<Layout title="利用規約 | manpuc" lang="ja">
    <div class="back-nav">
        <a href="/" class="back-btn" aria-label="Back to home">
            <svg
                width="20"
                height="20"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2.5"
                stroke-linecap="round"
                stroke-linejoin="round"
                class="back-btn-icon"
            >
                <line x1="19" y1="12" x2="5" y2="12"></line>
                <polyline points="12 19 5 12 12 5"></polyline>
            </svg>

            <span class="text-long"> ホームに戻る </span>

            <span class="text-short"> ホーム </span>
        </a>
    </div>

    <div class="container" style="max-width: 800px;">
        <header class="reveal">
            <h1>利用規約</h1>
            <p class="subtitle">Terms of Service</p>
        </header>

        <section class="glass-card reveal">
            <h2>第1条（適用）</h2>
            <p>本規約は、制作者「manpuc」（以下、「制作者」）が当ポートフォリオサイト（以下、「当サイト」）上で提供するサービス、および公開しているコンテンツの利用条件を定めるものです。ユーザーが当サイトを利用した時点で、本規約に同意したものとみなします。</p>
        </section>

        <section class="glass-card reveal">
            <h2>第2条（禁止事項）</h2>
            <p>ユーザーは、当サイトの利用にあたり、以下の行為をしてはなりません。</p>
            <ul>
                <li>当サイトの運営を妨害する行為、またはその恐れのある行為。</li>
                <li>制作者または第三者の著作権、商標権等の知的財産権を侵害する行為。</li>
                <li>不正アクセス、または当サイトのサーバーやネットワークの機能を破壊する行為。</li>
                <li>制作者の許可なく、当サイトのコンテンツを商用利用（二次配布、再販売等）する行為。</li>
                <li>その他、法令または公序良俗に違反する行為。</li>
            </ul>
        </section>

        <section class="glass-card reveal">
            <h2>第3条（免責事項）</h2>
            <p>制作者は、当サイトに掲載されている情報について、その正確性、完全性、最新性を保証するものではありません。</p>
            <p>当サイトの利用によって生じた損害（直接・間接を問いません）について、制作者は一切の責任を負わないものとします。公開されているソースコードやソフトウェアの利用は、自己責任で行ってください。</p>
        </section>

        <section class="glass-card reveal">
            <h2>第4条（著作権）</h2>
            <p>当サイトに掲載されている全てのコンテンツ（テキスト、画像、公開されているコード等）の著作権は、特段の表記がない限り制作者に帰属します。私的使用の範囲を超えて、無断で複製、転載、加工、配布等を行うことはできません。</p>
        </section>

        <section class="glass-card reveal">
            <h2>第5条（規約の変更）</h2>
            <p>制作者は、必要と判断した場合には、ユーザーに通知することなくいつでも本規約を変更することができるものとします。変更後の規約は、当サイトに掲示した時点から効力を生じるものとします。</p>
        </section>

        <div class="view-more-container reveal">
            <a href="/" class="back-btn centered">
                <svg
                    width="20"
                    height="20"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2.5"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    class="back-btn-icon"
                >
                    <line x1="19" y1="12" x2="5" y2="12"></line>
                    <polyline points="12 19 5 12 12 5"></polyline>
                </svg>
                ホームに戻る
            </a>
        </div>
    </div>
</Layout>

<style>
    section {
        margin-bottom: 2rem;
    }
    p {
        color: var(--txt-sub);
        margin-bottom: 1rem;
        line-height: 1.8;
    }
    ul {
        color: var(--txt-sub);
        margin-bottom: 1rem;
        padding-left: 1.5rem;
    }
    li {
        margin-bottom: 0.5rem;
    }

    /* --- Navigation (Back to Home) --- */
    .back-nav {
        position: fixed;
        top: 30px;
        left: 30px;
        z-index: 1000;
    }

    .back-btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        height: 65px;
        width: 210px;
        padding: 0 28px;
        gap: 10px;
        background: var(--card-bg);
        color: var(--txt-main);
        text-decoration: none;
        border-radius: 50px;
        font-weight: 700;
        border: 1px solid var(--border);
        box-shadow: 0 10px 30px var(--shadow);
        overflow: hidden;
        white-space: nowrap;
        position: relative;
        isolation: isolate;
        backdrop-filter: blur(10px) !important;
        -webkit-backdrop-filter: blur(10px) !important;
        transition: all 0.4s cubic-bezier(0.65, 0, 0.35, 1);
    }

    /* Centered version for bottom navigation */
    .back-btn.centered {
        position: relative;
        margin: 0 auto;
    }

    /* すりガラス効果：直接定義 */
    .back-btn::after {
        content: "";
        position: absolute;
        inset: 0;
        z-index: -1;
        border-radius: inherit;
        pointer-events: none;
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        filter: url(#glass-distortion);
        -webkit-filter: url(#glass-distortion);
    }

    .back-btn::before {
        content: "";
        position: absolute;
        inset: 0;
        z-index: 0;
        border-radius: inherit;
        box-shadow: inset 0 0 6px -3px rgba(255, 255, 255, 0.7);
        pointer-events: none;
    }

    .back-btn-icon {
        width: 20px !important;
        height: 20px !important;
        flex-shrink: 0;
        transition: all 0.4s cubic-bezier(0.65, 0, 0.35, 1);
    }

    .text-short {
        display: none;
    }

    .back-btn:hover {
        background: var(--blu);
        color: #fff;
        transform: scale(1.05);
        border-color: var(--blu);
    }

    .back-btn:active {
        transform: scale(0.95);
    }

    .back-btn svg {
        transition: all 0.6s cubic-bezier(0.65, 0, 0.35, 1);
    }

    .back-btn:hover svg {
        transform: translateX(-4px);
    }

    @media (max-width: 480px) {
        .back-btn {
            width: 140px;
            padding: 0 25px;
        }
        .text-long {
            display: none;
        }
        .text-short {
            display: inline;
        }
    }

    @media (max-width: 360px) {
        .back-btn {
            width: 65px;
            padding: 0;
            gap: 0;
            border-radius: 35px;
        }
        .text-short {
            display: none;
        }

        .back-btn-icon {
            width: 24px;
            height: 24px;
        }
    }
</style>

